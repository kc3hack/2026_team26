name: Desktop CI

on:
  push:
    branches: ["**"]
    paths:
      - "desktop/**"

jobs:
  desktop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check for desktop project
        run: |
          if [ -d "desktop" ]; then
            echo "desktop directory exists — add build steps here"
            # add real build/test steps for the desktop project
            exit 0
          else
            echo "No desktop project found; nothing to run."
            exit 0
          fi

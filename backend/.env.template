# Backend environment variables (copy to .env and edit)
# Port the app listens on
APP_PORT=8080

# PostgreSQL connection (used by docker-compose defaults)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=team26
DB_USER=team26
DB_PASSWORD=team26

# JWT secret for signing tokens (change in production)
JWT_SECRET=replace-me-with-secure-random

# Logging
LOG_LEVEL=info
